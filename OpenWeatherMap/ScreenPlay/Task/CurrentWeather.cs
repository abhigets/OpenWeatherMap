using System;
using RestSharp;

namespace OpenWeatherMap.ScreenPlay
{
    internal class CurrentWeather
    {
        RestClient restClient;
        internal void by(string cityName)
        {
            //restClient = new RestClient();
        }
    }
}